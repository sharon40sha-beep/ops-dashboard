# Supabase connection (client-side).
# The PUBLISHABLE key is designed to be exposed in the browser and is safe to
# commit for an internal tool. Real secrets (service_role) must never live here.
#
# To run locally:  cp .env.example .env
VITE_SUPABASE_URL=https://qqjfrluhlfvxkmlgjsim.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_Uo7jbOMpOckvZlToMVRbaQ_tOpKJieD
